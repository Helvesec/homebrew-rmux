# Homebrew Tap for RMUX

```sh
brew install Helvesec/rmux/rmux
```

Or:

```sh
brew tap Helvesec/rmux
brew install rmux
```

Linux users should use the APT/RPM repositories or the install script from
https://rmux.io.
